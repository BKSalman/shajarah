# [[language]]
# name = "rust"
# formatter = { command = "bash", args = ["-c", "rustfmt | dx fmt -f -"] }

[language-server.rust-analyzer.config.cargo]
features = ["server"]

[language-server.rust-analyzer.config.check]
targets = ["wasm32-unknown-unknown", "host-tuple"]
