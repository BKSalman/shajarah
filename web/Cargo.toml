[package]
name = "web"
version = "0.1.0"
authors = ["Salman Abuhaimed <salman.f.abuhaimed@gmail.com>"]
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
shared = { path = "../shared" }

dioxus = { version = "0.7.10", features = ["router", "fullstack"] }
# https://github.com/DioxusLabs/dioxus/issues/4765
dioxus-html = { version = "0.7.10", features = ["serialize"] }
serde = { workspace = true }
serde_json = { workspace = true }
jiff = { workspace = true }
indexmap = { workspace = true }
garde = { version = "0.23.0", features = ["derive", "email"] }
uuid = { version = "1.18.0", features = ["serde", "v4"] }
thiserror = "2.0.14"
tracing = "0.1.41"
base64 = "0.22.1"
url = { version = "2.5.4", features = ["serde"] }
tower = "0.5.2"
anyhow = { workspace = true }
http = "1.4.0"
gloo-timers = { version = "0.4.0", features = ["futures"] }
futures = "0.3.32"
web-sys.workspace = true
hex = "0.4.3"
sha2 = "0.11.0"

[target.wasm32-unknown-unknown.dependencies]
uuid = { version = "1.18.0", features = ["serde", "v4", "js"] }
gui = { path = "../gui" }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
axum = { version = "0.8.4", features = ["macros", "multipart"], optional = true }
tokio = { version = "1", features = [ "fs", "io-std", "io-util", "macros", "net", "parking_lot", "process", "rt", "rt-multi-thread", "signal" ], optional = true }
tower-http = { version = "0.6.6", features = ["cors", "limit", "fs", "compression-br", "compression-gzip"], optional = true }
sqlx = { version = "0.9", features = [ "runtime-tokio", "tls-rustls", "postgres", "uuid" ], optional = true }
jiff-sqlx = { version = "0.2.0", features = ["postgres"], optional = true }
tower-cookies = { version = "0.11.0", optional = true, features = ["private"] }
rand = { version = "0.9.2", optional = true }
argon2 = { version = "0.5.3", optional = true }
async-trait = { version = "0.1.88", optional = true }
toml = { version = "0.9.5", optional = true }
aes-gcm = { version = "0.10.3", optional = true }
csv = { version = "1.3.1", optional = true }


[features]
default = ["web"]
# The feature that are only required for the web = ["dioxus/web"] build target should be optional and only enabled in the web = ["dioxus/web"] feature
web = ["dioxus/web"]
# The feature that are only required for the desktop = ["dioxus/desktop"] build target should be optional and only enabled in the desktop = ["dioxus/desktop"] feature
desktop = ["dioxus/desktop"]
# The feature that are only required for the mobile = ["dioxus/mobile"] build target should be optional and only enabled in the mobile = ["dioxus/mobile"] feature
mobile = ["dioxus/mobile"]
server = [
  "dioxus/server",
  "dep:axum",
  "dep:tokio",
  "dep:tower-http",
  "dep:sqlx",
  "dep:jiff-sqlx",
  "dep:tower-cookies",
  "dep:rand",
  "dep:argon2",
  "dep:async-trait",
  "dep:toml",
  "dep:aes-gcm",
  "dep:csv"
]
